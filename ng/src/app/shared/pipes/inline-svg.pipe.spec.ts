import { InlineSvgPipe } from './inline-svg.pipe';

describe('InlineSvgPipe', () => {
  it('create an instance', () => {
    const pipe = new InlineSvgPipe();
    expect(pipe).toBeTruthy();
  });
});
